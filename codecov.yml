comment:
  layout: header, changes, diff, sunburst, uncovered
coverage:
  status:
    patch:
      default:
        target: '85'
    project:
      default:
target: auto

codecov:
  ci:
    - dev.azure.com
